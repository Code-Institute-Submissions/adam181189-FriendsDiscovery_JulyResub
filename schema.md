```BEGIN TRANSACTION;
CREATE TABLE IF NOT EXISTS "django_migrations" (
	"id"	integer NOT NULL,
	"app"	varchar(255) NOT NULL,
	"name"	varchar(255) NOT NULL,
	"applied"	datetime NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "auth_group_permissions" (
	"id"	integer NOT NULL,
	"group_id"	integer NOT NULL,
	"permission_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("permission_id") REFERENCES "auth_permission"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("group_id") REFERENCES "auth_group"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "auth_user_groups" (
	"id"	integer NOT NULL,
	"user_id"	integer NOT NULL,
	"group_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("group_id") REFERENCES "auth_group"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "auth_user_user_permissions" (
	"id"	integer NOT NULL,
	"user_id"	integer NOT NULL,
	"permission_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("permission_id") REFERENCES "auth_permission"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "account_emailconfirmation" (
	"id"	integer NOT NULL,
	"created"	datetime NOT NULL,
	"sent"	datetime,
	"key"	varchar(64) NOT NULL UNIQUE,
	"email_address_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("email_address_id") REFERENCES "account_emailaddress"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "account_emailaddress" (
	"id"	integer NOT NULL,
	"verified"	bool NOT NULL,
	"primary"	bool NOT NULL,
	"user_id"	integer NOT NULL,
	"email"	varchar(254) NOT NULL UNIQUE,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "django_admin_log" (
	"id"	integer NOT NULL,
	"action_time"	datetime NOT NULL,
	"object_id"	text,
	"object_repr"	varchar(200) NOT NULL,
	"change_message"	text NOT NULL,
	"content_type_id"	integer,
	"user_id"	integer NOT NULL,
	"action_flag"	smallint unsigned NOT NULL CHECK("action_flag" >= 0),
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("content_type_id") REFERENCES "django_content_type"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "django_content_type" (
	"id"	integer NOT NULL,
	"app_label"	varchar(100) NOT NULL,
	"model"	varchar(100) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "auth_permission" (
	"id"	integer NOT NULL,
	"content_type_id"	integer NOT NULL,
	"codename"	varchar(100) NOT NULL,
	"name"	varchar(255) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("content_type_id") REFERENCES "django_content_type"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "auth_group" (
	"id"	integer NOT NULL,
	"name"	varchar(150) NOT NULL UNIQUE,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "auth_user" (
	"id"	integer NOT NULL,
	"password"	varchar(128) NOT NULL,
	"last_login"	datetime,
	"is_superuser"	bool NOT NULL,
	"username"	varchar(150) NOT NULL UNIQUE,
	"last_name"	varchar(150) NOT NULL,
	"email"	varchar(254) NOT NULL,
	"is_staff"	bool NOT NULL,
	"is_active"	bool NOT NULL,
	"date_joined"	datetime NOT NULL,
	"first_name"	varchar(150) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "django_session" (
	"session_key"	varchar(40) NOT NULL,
	"session_data"	text NOT NULL,
	"expire_date"	datetime NOT NULL,
	PRIMARY KEY("session_key")
);
CREATE TABLE IF NOT EXISTS "django_site" (
	"id"	integer NOT NULL,
	"name"	varchar(50) NOT NULL,
	"domain"	varchar(100) NOT NULL UNIQUE,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "socialaccount_socialapp_sites" (
	"id"	integer NOT NULL,
	"socialapp_id"	integer NOT NULL,
	"site_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("socialapp_id") REFERENCES "socialaccount_socialapp"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("site_id") REFERENCES "django_site"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "socialaccount_socialtoken" (
	"id"	integer NOT NULL,
	"token"	text NOT NULL,
	"token_secret"	text NOT NULL,
	"expires_at"	datetime,
	"account_id"	integer NOT NULL,
	"app_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("app_id") REFERENCES "socialaccount_socialapp"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("account_id") REFERENCES "socialaccount_socialaccount"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "socialaccount_socialapp" (
	"id"	integer NOT NULL,
	"provider"	varchar(30) NOT NULL,
	"name"	varchar(40) NOT NULL,
	"client_id"	varchar(191) NOT NULL,
	"key"	varchar(191) NOT NULL,
	"secret"	varchar(191) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "socialaccount_socialaccount" (
	"id"	integer NOT NULL,
	"provider"	varchar(30) NOT NULL,
	"uid"	varchar(191) NOT NULL,
	"last_login"	datetime NOT NULL,
	"date_joined"	datetime NOT NULL,
	"user_id"	integer NOT NULL,
	"extra_data"	text NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "profilepage_updateinfo" (
	"id"	integer NOT NULL,
	"date_of_birth"	date NOT NULL,
	"bio"	text NOT NULL,
	"gender"	varchar(13) NOT NULL,
	"user_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "friendship_follow" (
	"id"	integer NOT NULL,
	"created"	datetime NOT NULL,
	"followee_id"	integer NOT NULL,
	"follower_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("followee_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("follower_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "friendship_friend" (
	"id"	integer NOT NULL,
	"created"	datetime NOT NULL,
	"from_user_id"	integer NOT NULL,
	"to_user_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("to_user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("from_user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "friendship_friendshiprequest" (
	"id"	integer NOT NULL,
	"message"	text NOT NULL,
	"created"	datetime NOT NULL,
	"rejected"	datetime,
	"viewed"	datetime,
	"from_user_id"	integer NOT NULL,
	"to_user_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("from_user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("to_user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "friendship_block" (
	"id"	integer NOT NULL,
	"created"	datetime NOT NULL,
	"blocked_id"	integer NOT NULL,
	"blocker_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("blocked_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("blocker_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "django_celery_beat_intervalschedule" (
	"id"	integer NOT NULL,
	"every"	integer NOT NULL,
	"period"	varchar(24) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "django_celery_beat_periodictasks" (
	"ident"	smallint NOT NULL,
	"last_update"	datetime NOT NULL,
	PRIMARY KEY("ident")
);
CREATE TABLE IF NOT EXISTS "django_celery_beat_periodictask" (
	"id"	integer NOT NULL,
	"name"	varchar(200) NOT NULL UNIQUE,
	"task"	varchar(200) NOT NULL,
	"args"	text NOT NULL,
	"kwargs"	text NOT NULL,
	"queue"	varchar(200),
	"exchange"	varchar(200),
	"routing_key"	varchar(200),
	"expires"	datetime,
	"enabled"	bool NOT NULL,
	"last_run_at"	datetime,
	"total_run_count"	integer unsigned NOT NULL CHECK("total_run_count" >= 0),
	"date_changed"	datetime NOT NULL,
	"description"	text NOT NULL,
	"crontab_id"	integer,
	"interval_id"	integer,
	"solar_id"	integer,
	"one_off"	bool NOT NULL,
	"start_time"	datetime,
	"priority"	integer unsigned CHECK("priority" >= 0),
	"headers"	text NOT NULL,
	"clocked_id"	integer,
	"expire_seconds"	integer unsigned CHECK("expire_seconds" >= 0),
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("crontab_id") REFERENCES "django_celery_beat_crontabschedule"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("solar_id") REFERENCES "django_celery_beat_solarschedule"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("clocked_id") REFERENCES "django_celery_beat_clockedschedule"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("interval_id") REFERENCES "django_celery_beat_intervalschedule"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "django_celery_beat_crontabschedule" (
	"id"	integer NOT NULL,
	"minute"	varchar(240) NOT NULL,
	"hour"	varchar(96) NOT NULL,
	"day_of_week"	varchar(64) NOT NULL,
	"day_of_month"	varchar(124) NOT NULL,
	"month_of_year"	varchar(64) NOT NULL,
	"timezone"	varchar(63) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "django_celery_beat_clockedschedule" (
	"id"	integer NOT NULL,
	"clocked_time"	datetime NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "django_celery_beat_solarschedule" (
	"id"	integer NOT NULL,
	"latitude"	decimal NOT NULL,
	"longitude"	decimal NOT NULL,
	"event"	varchar(24) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "djstripe_webhookeventtrigger" (
	"id"	integer NOT NULL,
	"remote_ip"	char(39) NOT NULL,
	"headers"	text NOT NULL,
	"body"	text NOT NULL,
	"valid"	bool NOT NULL,
	"processed"	bool NOT NULL,
	"exception"	varchar(128) NOT NULL,
	"traceback"	text NOT NULL,
	"djstripe_version"	varchar(32) NOT NULL,
	"created"	datetime NOT NULL,
	"updated"	datetime NOT NULL,
	"event_id"	varchar(255),
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("event_id") REFERENCES "djstripe_event"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_idempotencykey" (
	"uuid"	char(32) NOT NULL,
	"action"	varchar(100) NOT NULL,
	"livemode"	bool NOT NULL,
	"created"	datetime NOT NULL,
	PRIMARY KEY("uuid")
);
CREATE TABLE IF NOT EXISTS "djstripe_djstripeinvoicedefaulttaxrate" (
	"id"	integer NOT NULL,
	"invoice_id"	bigint NOT NULL,
	"taxrate_id"	bigint NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("invoice_id") REFERENCES "djstripe_invoice"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("taxrate_id") REFERENCES "djstripe_taxrate"("djstripe_id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_djstripeinvoiceitemtaxrate" (
	"id"	integer NOT NULL,
	"invoiceitem_id"	bigint NOT NULL,
	"taxrate_id"	bigint NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("taxrate_id") REFERENCES "djstripe_taxrate"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("invoiceitem_id") REFERENCES "djstripe_invoiceitem"("djstripe_id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_djstripesubscriptiondefaulttaxrate" (
	"id"	integer NOT NULL,
	"subscription_id"	bigint NOT NULL,
	"taxrate_id"	bigint NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("subscription_id") REFERENCES "djstripe_subscription"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("taxrate_id") REFERENCES "djstripe_taxrate"("djstripe_id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_djstripesubscriptionitemtaxrate" (
	"id"	integer NOT NULL,
	"subscriptionitem_id"	bigint NOT NULL,
	"taxrate_id"	bigint NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("taxrate_id") REFERENCES "djstripe_taxrate"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("subscriptionitem_id") REFERENCES "djstripe_subscriptionitem"("djstripe_id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_djstripeupcominginvoicetotaltaxamount" (
	"id"	integer NOT NULL,
	"amount"	bigint NOT NULL,
	"inclusive"	bool NOT NULL,
	"invoice_id"	bigint NOT NULL,
	"tax_rate_id"	varchar(255) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("invoice_id") REFERENCES "djstripe_upcominginvoice"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("tax_rate_id") REFERENCES "djstripe_taxrate"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_djstripeinvoicetotaltaxamount" (
	"id"	integer NOT NULL,
	"amount"	bigint NOT NULL,
	"inclusive"	bool NOT NULL,
	"invoice_id"	varchar(255) NOT NULL,
	"tax_rate_id"	varchar(255) NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("invoice_id") REFERENCES "djstripe_invoice"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("tax_rate_id") REFERENCES "djstripe_taxrate"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_djstripepaymentmethod" (
	"id"	varchar(255) NOT NULL,
	"type"	varchar(50) NOT NULL,
	PRIMARY KEY("id")
);
CREATE TABLE IF NOT EXISTS "djstripe_applicationfee" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	bigint NOT NULL,
	"amount_refunded"	bigint NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"refunded"	bool NOT NULL,
	"balance_transaction_id"	varchar(255) NOT NULL,
	"charge_id"	varchar(255) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("charge_id") REFERENCES "djstripe_charge"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_applicationfeerefund" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	bigint NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"balance_transaction_id"	varchar(255) NOT NULL,
	"fee_id"	varchar(255) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("fee_id") REFERENCES "djstripe_applicationfee"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_balancetransaction" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	bigint NOT NULL,
	"available_on"	datetime NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"exchange_rate"	decimal,
	"fee"	bigint NOT NULL,
	"fee_details"	text NOT NULL,
	"net"	bigint NOT NULL,
	"status"	varchar(9) NOT NULL,
	"type"	varchar(29) NOT NULL,
	"reporting_category"	varchar(29) NOT NULL,
	"source"	varchar(255) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_card" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"address_city"	text NOT NULL,
	"address_country"	text NOT NULL,
	"address_line1"	text NOT NULL,
	"address_line1_check"	varchar(11) NOT NULL,
	"address_line2"	text NOT NULL,
	"address_state"	text NOT NULL,
	"address_zip"	text NOT NULL,
	"address_zip_check"	varchar(11) NOT NULL,
	"brand"	varchar(16) NOT NULL,
	"country"	varchar(2) NOT NULL,
	"cvc_check"	varchar(11) NOT NULL,
	"dynamic_last4"	varchar(4) NOT NULL,
	"exp_month"	integer NOT NULL,
	"exp_year"	integer NOT NULL,
	"fingerprint"	varchar(16) NOT NULL,
	"funding"	varchar(7) NOT NULL,
	"last4"	varchar(4) NOT NULL,
	"name"	text NOT NULL,
	"tokenization_method"	varchar(11) NOT NULL,
	"customer_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_countryspec" (
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"id"	varchar(2) NOT NULL,
	"default_currency"	varchar(3) NOT NULL,
	"supported_bank_account_currencies"	text NOT NULL,
	"supported_payment_currencies"	text NOT NULL,
	"supported_payment_methods"	text NOT NULL,
	"supported_transfer_countries"	text NOT NULL,
	"verification_fields"	text NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("id"),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_coupon" (
	"djstripe_id"	integer NOT NULL,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"id"	varchar(500) NOT NULL,
	"amount_off"	decimal,
	"currency"	varchar(3),
	"duration"	varchar(9) NOT NULL,
	"duration_in_months"	integer unsigned CHECK("duration_in_months" >= 0),
	"max_redemptions"	integer unsigned CHECK("max_redemptions" >= 0),
	"percent_off"	decimal,
	"redeem_by"	datetime,
	"times_redeemed"	integer unsigned NOT NULL CHECK("times_redeemed" >= 0),
	"name"	text NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_dispute" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	bigint NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"evidence"	text NOT NULL,
	"evidence_details"	text NOT NULL,
	"is_charge_refundable"	bool NOT NULL,
	"reason"	varchar(25) NOT NULL,
	"status"	varchar(22) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_event" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"api_version"	varchar(15) NOT NULL,
	"data"	text NOT NULL,
	"request_id"	varchar(50) NOT NULL,
	"idempotency_key"	text NOT NULL,
	"type"	varchar(250) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_fileupload" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"filename"	varchar(255) NOT NULL,
	"purpose"	varchar(35) NOT NULL,
	"size"	integer NOT NULL,
	"type"	varchar(4) NOT NULL,
	"url"	varchar(200) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_invoice" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount_due"	decimal NOT NULL,
	"amount_paid"	decimal,
	"amount_remaining"	decimal,
	"attempt_count"	integer NOT NULL,
	"attempted"	bool NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"due_date"	datetime,
	"ending_balance"	bigint,
	"hosted_invoice_url"	text NOT NULL,
	"invoice_pdf"	text NOT NULL,
	"next_payment_attempt"	datetime,
	"number"	varchar(64) NOT NULL,
	"paid"	bool NOT NULL,
	"period_end"	datetime NOT NULL,
	"period_start"	datetime NOT NULL,
	"receipt_number"	varchar(64),
	"starting_balance"	bigint NOT NULL,
	"statement_descriptor"	varchar(22) NOT NULL,
	"subscription_proration_date"	datetime,
	"subtotal"	decimal NOT NULL,
	"tax"	decimal,
	"tax_percent"	decimal,
	"total"	decimal NOT NULL,
	"webhooks_delivered_at"	datetime,
	"charge_id"	bigint UNIQUE,
	"customer_id"	varchar(255) NOT NULL,
	"subscription_id"	varchar(255),
	"auto_advance"	bool,
	"application_fee_amount"	decimal,
	"status_transitions"	text,
	"payment_intent_id"	bigint UNIQUE,
	"collection_method"	varchar(20),
	"account_country"	varchar(2) NOT NULL,
	"account_name"	text NOT NULL,
	"billing_reason"	varchar(22) NOT NULL,
	"customer_address"	text,
	"customer_email"	text NOT NULL,
	"customer_name"	text NOT NULL,
	"customer_phone"	text NOT NULL,
	"customer_shipping"	text,
	"customer_tax_exempt"	varchar(7) NOT NULL,
	"default_payment_method_id"	varchar(255),
	"footer"	text NOT NULL,
	"post_payment_credit_notes_amount"	bigint,
	"pre_payment_credit_notes_amount"	bigint,
	"threshold_reason"	text,
	"status"	varchar(13) NOT NULL,
	"discount"	text,
	"default_source_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("subscription_id") REFERENCES "djstripe_subscription"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("payment_intent_id") REFERENCES "djstripe_paymentintent"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("default_source_id") REFERENCES "djstripe_djstripepaymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("default_payment_method_id") REFERENCES "djstripe_paymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("charge_id") REFERENCES "djstripe_charge"("djstripe_id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_paymentintent" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	bigint NOT NULL,
	"amount_capturable"	bigint NOT NULL,
	"amount_received"	bigint NOT NULL,
	"canceled_at"	datetime,
	"cancellation_reason"	varchar(21) NOT NULL,
	"capture_method"	varchar(9) NOT NULL,
	"client_secret"	text NOT NULL,
	"confirmation_method"	varchar(9) NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"description"	text NOT NULL,
	"last_payment_error"	text,
	"next_action"	text,
	"payment_method_types"	text NOT NULL,
	"receipt_email"	varchar(255) NOT NULL,
	"setup_future_usage"	varchar(11),
	"shipping"	text,
	"statement_descriptor"	varchar(22) NOT NULL,
	"status"	varchar(23) NOT NULL,
	"transfer_data"	text,
	"transfer_group"	varchar(255) NOT NULL,
	"customer_id"	varchar(255),
	"on_behalf_of_id"	varchar(255),
	"payment_method_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("payment_method_id") REFERENCES "djstripe_paymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("on_behalf_of_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_product" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"name"	text NOT NULL,
	"type"	varchar(7) NOT NULL,
	"active"	bool,
	"attributes"	text,
	"caption"	text NOT NULL,
	"deactivate_on"	text,
	"images"	text,
	"package_dimensions"	text,
	"shippable"	bool,
	"url"	varchar(799),
	"statement_descriptor"	varchar(22) NOT NULL,
	"unit_label"	varchar(12) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_refund" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	bigint NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"failure_reason"	varchar(24) NOT NULL,
	"reason"	varchar(25) NOT NULL,
	"receipt_number"	varchar(9) NOT NULL,
	"status"	varchar(9) NOT NULL,
	"charge_id"	varchar(255) NOT NULL,
	"balance_transaction_id"	varchar(255),
	"failure_balance_transaction_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("charge_id") REFERENCES "djstripe_charge"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("failure_balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_scheduledqueryrun" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"data_load_time"	datetime NOT NULL,
	"error"	text,
	"result_available_until"	datetime NOT NULL,
	"sql"	text NOT NULL,
	"status"	varchar(9) NOT NULL,
	"title"	text NOT NULL,
	"file_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("file_id") REFERENCES "djstripe_fileupload"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_session" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"billing_address_collection"	varchar(8) NOT NULL,
	"cancel_url"	text NOT NULL,
	"client_reference_id"	text NOT NULL,
	"customer_email"	varchar(255) NOT NULL,
	"display_items"	text,
	"locale"	varchar(255) NOT NULL,
	"payment_method_types"	text NOT NULL,
	"submit_type"	varchar(6) NOT NULL,
	"success_url"	text NOT NULL,
	"customer_id"	varchar(255),
	"payment_intent_id"	varchar(255),
	"subscription_id"	varchar(255),
	"mode"	varchar(12) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("subscription_id") REFERENCES "djstripe_subscription"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("payment_intent_id") REFERENCES "djstripe_paymentintent"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_setupintent" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"application"	varchar(255) NOT NULL,
	"cancellation_reason"	varchar(21) NOT NULL,
	"client_secret"	text NOT NULL,
	"last_setup_error"	text,
	"next_action"	text,
	"payment_method_types"	text NOT NULL,
	"status"	varchar(23) NOT NULL,
	"usage"	varchar(11) NOT NULL,
	"customer_id"	varchar(255),
	"on_behalf_of_id"	varchar(255),
	"payment_method_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("on_behalf_of_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("payment_method_id") REFERENCES "djstripe_paymentmethod"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_source" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	decimal,
	"client_secret"	varchar(255) NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"flow"	varchar(17) NOT NULL,
	"owner"	text NOT NULL,
	"statement_descriptor"	varchar(255) NOT NULL,
	"status"	varchar(10) NOT NULL,
	"type"	varchar(20) NOT NULL,
	"usage"	varchar(10) NOT NULL,
	"code_verification"	text,
	"receiver"	text,
	"redirect"	text,
	"source_data"	text NOT NULL,
	"customer_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_taxrate" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"active"	bool NOT NULL,
	"display_name"	varchar(50) NOT NULL,
	"inclusive"	bool NOT NULL,
	"jurisdiction"	varchar(50) NOT NULL,
	"percentage"	decimal NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_transfer" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	decimal NOT NULL,
	"amount_reversed"	decimal,
	"currency"	varchar(3) NOT NULL,
	"destination"	varchar(255) NOT NULL,
	"destination_payment"	varchar(255),
	"reversed"	bool NOT NULL,
	"source_transaction"	varchar(255),
	"source_type"	varchar(16) NOT NULL,
	"transfer_group"	varchar(255) NOT NULL,
	"balance_transaction_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_transferreversal" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	bigint NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"balance_transaction_id"	varchar(255),
	"transfer_id"	varchar(255) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("transfer_id") REFERENCES "djstripe_transfer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_upcominginvoice" (
	"djstripe_id"	integer NOT NULL,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"account_country"	varchar(2) NOT NULL,
	"account_name"	text NOT NULL,
	"amount_due"	decimal NOT NULL,
	"amount_paid"	decimal,
	"amount_remaining"	decimal,
	"application_fee_amount"	decimal,
	"attempt_count"	integer NOT NULL,
	"attempted"	bool NOT NULL,
	"auto_advance"	bool,
	"billing_reason"	varchar(22) NOT NULL,
	"collection_method"	varchar(20),
	"currency"	varchar(3) NOT NULL,
	"customer_address"	text,
	"customer_email"	text NOT NULL,
	"customer_name"	text NOT NULL,
	"customer_phone"	text NOT NULL,
	"customer_shipping"	text,
	"customer_tax_exempt"	varchar(7) NOT NULL,
	"due_date"	datetime,
	"ending_balance"	bigint,
	"footer"	text NOT NULL,
	"hosted_invoice_url"	text NOT NULL,
	"invoice_pdf"	text NOT NULL,
	"next_payment_attempt"	datetime,
	"number"	varchar(64) NOT NULL,
	"paid"	bool NOT NULL,
	"period_end"	datetime NOT NULL,
	"period_start"	datetime NOT NULL,
	"post_payment_credit_notes_amount"	bigint,
	"pre_payment_credit_notes_amount"	bigint,
	"receipt_number"	varchar(64),
	"starting_balance"	bigint NOT NULL,
	"statement_descriptor"	varchar(22) NOT NULL,
	"status_transitions"	text,
	"subscription_proration_date"	datetime,
	"subtotal"	decimal NOT NULL,
	"tax"	decimal,
	"tax_percent"	decimal,
	"threshold_reason"	text,
	"total"	decimal NOT NULL,
	"webhooks_delivered_at"	datetime,
	"charge_id"	bigint UNIQUE,
	"customer_id"	varchar(255) NOT NULL,
	"default_payment_method_id"	varchar(255),
	"payment_intent_id"	bigint UNIQUE,
	"subscription_id"	varchar(255),
	"status"	varchar(13) NOT NULL,
	"default_source_id"	varchar(255),
	"discount"	text,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("charge_id") REFERENCES "djstripe_charge"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("payment_intent_id") REFERENCES "djstripe_paymentintent"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("default_payment_method_id") REFERENCES "djstripe_paymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("subscription_id") REFERENCES "djstripe_subscription"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("default_source_id") REFERENCES "djstripe_djstripepaymentmethod"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_usagerecord" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"quantity"	integer unsigned NOT NULL CHECK("quantity" >= 0),
	"subscription_item_id"	varchar(255) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("subscription_item_id") REFERENCES "djstripe_subscriptionitem"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_account" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"charges_enabled"	bool NOT NULL,
	"country"	varchar(2) NOT NULL,
	"default_currency"	varchar(3) NOT NULL,
	"details_submitted"	bool NOT NULL,
	"email"	varchar(255) NOT NULL,
	"payouts_enabled"	bool,
	"product_description"	varchar(255) NOT NULL,
	"type"	varchar(8) NOT NULL,
	"tos_acceptance"	text,
	"business_profile"	text,
	"business_type"	varchar(10) NOT NULL,
	"company"	text,
	"individual"	text,
	"requirements"	text,
	"settings"	text,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "djstripe_bankaccount" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"account_holder_type"	varchar(10) NOT NULL,
	"bank_name"	varchar(255) NOT NULL,
	"country"	varchar(2) NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"default_for_currency"	bool,
	"fingerprint"	varchar(16) NOT NULL,
	"last4"	varchar(4) NOT NULL,
	"routing_number"	varchar(255) NOT NULL,
	"status"	varchar(19) NOT NULL,
	"account_id"	varchar(255),
	"customer_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	"account_holder_name"	text NOT NULL,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_price" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"active"	bool NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"nickname"	varchar(250) NOT NULL,
	"recurring"	text,
	"type"	varchar(9) NOT NULL,
	"unit_amount"	bigint,
	"unit_amount_decimal"	decimal,
	"billing_scheme"	varchar(8) NOT NULL,
	"tiers"	text,
	"tiers_mode"	varchar(9),
	"djstripe_owner_account_id"	varchar(255),
	"product_id"	varchar(255) NOT NULL,
	"lookup_key"	varchar(250),
	"transform_quantity"	text,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("product_id") REFERENCES "djstripe_product"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_taxid" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"country"	varchar(2) NOT NULL,
	"type"	varchar(7) NOT NULL,
	"value"	varchar(50) NOT NULL,
	"verification"	text NOT NULL,
	"customer_id"	varchar(255) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_customer" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"delinquent"	bool NOT NULL,
	"coupon_start"	datetime,
	"coupon_end"	datetime,
	"email"	text NOT NULL,
	"shipping"	text,
	"date_purged"	datetime,
	"coupon_id"	bigint,
	"default_source_id"	varchar(255),
	"subscriber_id"	integer,
	"balance"	bigint NOT NULL,
	"address"	text,
	"invoice_prefix"	varchar(255) NOT NULL,
	"invoice_settings"	text,
	"name"	text NOT NULL,
	"phone"	text NOT NULL,
	"preferred_locales"	text,
	"tax_exempt"	varchar(7) NOT NULL,
	"default_payment_method_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("coupon_id") REFERENCES "djstripe_coupon"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("default_payment_method_id") REFERENCES "djstripe_paymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("default_source_id") REFERENCES "djstripe_djstripepaymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("subscriber_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_invoiceitem" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	decimal NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"date"	datetime NOT NULL,
	"discountable"	bool NOT NULL,
	"period"	text NOT NULL,
	"period_end"	datetime NOT NULL,
	"period_start"	datetime NOT NULL,
	"proration"	bool NOT NULL,
	"quantity"	integer,
	"customer_id"	varchar(255) NOT NULL,
	"invoice_id"	varchar(255),
	"plan_id"	bigint,
	"subscription_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	"unit_amount"	bigint,
	"unit_amount_decimal"	decimal,
	"price_id"	bigint,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("plan_id") REFERENCES "djstripe_plan"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("price_id") REFERENCES "djstripe_price"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("subscription_id") REFERENCES "djstripe_subscription"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("invoice_id") REFERENCES "djstripe_invoice"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_subscriptionschedule" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"canceled_at"	datetime,
	"completed_at"	datetime,
	"current_phase"	text,
	"default_settings"	text,
	"end_behavior"	varchar(7) NOT NULL,
	"phases"	text,
	"released_at"	datetime,
	"status"	varchar(11) NOT NULL,
	"customer_id"	bigint NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	"released_subscription_id"	bigint,
	"billing_thresholds"	text,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("released_subscription_id") REFERENCES "djstripe_subscription"("djstripe_id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_payout" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	decimal NOT NULL,
	"arrival_date"	datetime NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"failure_code"	varchar(23) NOT NULL,
	"failure_message"	text NOT NULL,
	"method"	varchar(8) NOT NULL,
	"statement_descriptor"	varchar(255) NOT NULL,
	"status"	varchar(10) NOT NULL,
	"type"	varchar(12) NOT NULL,
	"destination_id"	varchar(255),
	"balance_transaction_id"	varchar(255),
	"failure_balance_transaction_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	"automatic"	bool NOT NULL,
	"source_type"	varchar(12) NOT NULL,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("destination_id") REFERENCES "djstripe_bankaccount"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("failure_balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_subscription" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"application_fee_percent"	decimal,
	"billing_cycle_anchor"	datetime,
	"cancel_at_period_end"	bool NOT NULL,
	"canceled_at"	datetime,
	"current_period_end"	datetime NOT NULL,
	"current_period_start"	datetime NOT NULL,
	"days_until_due"	integer,
	"ended_at"	datetime,
	"quantity"	integer,
	"status"	varchar(18) NOT NULL,
	"tax_percent"	decimal,
	"trial_end"	datetime,
	"trial_start"	datetime,
	"customer_id"	varchar(255) NOT NULL,
	"plan_id"	bigint,
	"pending_setup_intent_id"	varchar(255),
	"collection_method"	varchar(20) NOT NULL,
	"default_payment_method_id"	varchar(255),
	"discount"	text,
	"next_pending_invoice_item_invoice"	datetime,
	"pending_invoice_item_interval"	text,
	"pending_update"	text,
	"start_date"	datetime,
	"default_source_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	"cancel_at"	datetime,
	"schedule_id"	bigint,
	"billing_thresholds"	text,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("default_payment_method_id") REFERENCES "djstripe_paymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("pending_setup_intent_id") REFERENCES "djstripe_setupintent"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("plan_id") REFERENCES "djstripe_plan"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("schedule_id") REFERENCES "djstripe_subscriptionschedule"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("default_source_id") REFERENCES "djstripe_djstripepaymentmethod"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_subscriptionitem" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"quantity"	integer unsigned CHECK("quantity" >= 0),
	"plan_id"	bigint NOT NULL,
	"subscription_id"	varchar(255) NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	"price_id"	bigint,
	"billing_thresholds"	text,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("price_id") REFERENCES "djstripe_price"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("subscription_id") REFERENCES "djstripe_subscription"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("plan_id") REFERENCES "djstripe_plan"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_charge" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"amount"	decimal NOT NULL,
	"amount_refunded"	decimal NOT NULL,
	"captured"	bool NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"failure_code"	varchar(42) NOT NULL,
	"failure_message"	text NOT NULL,
	"fraud_details"	text,
	"outcome"	text,
	"paid"	bool NOT NULL,
	"receipt_email"	text NOT NULL,
	"receipt_number"	varchar(14) NOT NULL,
	"refunded"	bool NOT NULL,
	"shipping"	text,
	"statement_descriptor"	varchar(22),
	"status"	varchar(9) NOT NULL,
	"transfer_group"	varchar(255),
	"customer_id"	varchar(255),
	"dispute_id"	varchar(255),
	"invoice_id"	varchar(255),
	"source_id"	varchar(255),
	"transfer_id"	varchar(255),
	"balance_transaction_id"	varchar(255),
	"payment_intent_id"	varchar(255),
	"payment_method_details"	text,
	"receipt_url"	text NOT NULL,
	"payment_method_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	"on_behalf_of_id"	varchar(255),
	"amount_captured"	decimal,
	"application"	varchar(255) NOT NULL,
	"application_fee_amount"	decimal,
	"billing_details"	text,
	"calculated_statement_descriptor"	varchar(22) NOT NULL,
	"disputed"	bool NOT NULL,
	"source_transfer_id"	varchar(255),
	"statement_descriptor_suffix"	varchar(22),
	"transfer_data"	text,
	"application_fee_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("source_transfer_id") REFERENCES "djstripe_transfer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("balance_transaction_id") REFERENCES "djstripe_balancetransaction"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("source_id") REFERENCES "djstripe_djstripepaymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("payment_method_id") REFERENCES "djstripe_paymentmethod"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("payment_intent_id") REFERENCES "djstripe_paymentintent"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("application_fee_id") REFERENCES "djstripe_applicationfee"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("dispute_id") REFERENCES "djstripe_dispute"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("transfer_id") REFERENCES "djstripe_transfer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("on_behalf_of_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("invoice_id") REFERENCES "djstripe_invoice"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_plan" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"description"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"aggregate_usage"	varchar(18) NOT NULL,
	"amount"	decimal,
	"billing_scheme"	varchar(8) NOT NULL,
	"currency"	varchar(3) NOT NULL,
	"interval"	varchar(5) NOT NULL,
	"nickname"	text NOT NULL,
	"tiers"	text,
	"tiers_mode"	varchar(9),
	"transform_usage"	text,
	"trial_period_days"	integer,
	"usage_type"	varchar(8) NOT NULL,
	"product_id"	varchar(255),
	"active"	bool NOT NULL,
	"djstripe_owner_account_id"	varchar(255),
	"amount_decimal"	decimal,
	"interval_count"	integer unsigned CHECK("interval_count" >= 0),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("product_id") REFERENCES "djstripe_product"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_apikey" (
	"djstripe_id"	integer NOT NULL,
	"livemode"	bool NOT NULL,
	"created"	datetime,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"id"	varchar(255) NOT NULL,
	"type"	varchar(11) NOT NULL,
	"name"	varchar(100) NOT NULL,
	"secret"	varchar(128) NOT NULL UNIQUE,
	"djstripe_owner_account_id"	varchar(255),
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "djstripe_paymentmethod" (
	"djstripe_id"	integer NOT NULL,
	"id"	varchar(255) NOT NULL UNIQUE,
	"livemode"	bool,
	"created"	datetime,
	"metadata"	text,
	"djstripe_created"	datetime NOT NULL,
	"djstripe_updated"	datetime NOT NULL,
	"billing_details"	text NOT NULL,
	"card"	text,
	"card_present"	text,
	"customer_id"	varchar(255),
	"djstripe_owner_account_id"	varchar(255),
	"alipay"	text,
	"au_becs_debit"	text,
	"bacs_debit"	text,
	"bancontact"	text,
	"eps"	text,
	"fpx"	text,
	"giropay"	text,
	"ideal"	text,
	"interac_present"	text,
	"oxxo"	text,
	"p24"	text,
	"sepa_debit"	text,
	"sofort"	text,
	"type"	varchar(15) NOT NULL,
	PRIMARY KEY("djstripe_id" AUTOINCREMENT),
	FOREIGN KEY("customer_id") REFERENCES "djstripe_customer"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("djstripe_owner_account_id") REFERENCES "djstripe_account"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "profilepage_heart" (
	"id"	integer NOT NULL,
	"date_posted"	datetime NOT NULL,
	"from_user_id"	integer NOT NULL,
	"to_user_id"	integer NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("to_user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("from_user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "profilepage_post" (
	"id"	integer NOT NULL,
	"date_posted"	datetime NOT NULL,
	"user_id"	integer NOT NULL,
	"new_post"	text NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);
CREATE TABLE IF NOT EXISTS "users_userdetails" (
	"id"	integer NOT NULL,
	"premium_member"	bool NOT NULL,
	"date_of_birth"	date NOT NULL,
	"gender"	varchar(13) NOT NULL,
	"user_id"	integer UNIQUE,
	"profile_pic"	varchar(100) NOT NULL,
	"nationality"	varchar(2) NOT NULL,
	"received_hearts"	integer NOT NULL,
	"daily_given_hearts"	integer NOT NULL,
	"subscription_id"	bigint,
	"bio"	text NOT NULL,
	PRIMARY KEY("id" AUTOINCREMENT),
	FOREIGN KEY("subscription_id") REFERENCES "djstripe_subscription"("djstripe_id") DEFERRABLE INITIALLY DEFERRED,
	FOREIGN KEY("user_id") REFERENCES "auth_user"("id") DEFERRABLE INITIALLY DEFERRED
);```